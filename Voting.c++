
/*
 * Voting.c++
 *
 */

// includes
#include <cassert>
#include <iostream>
#include <string>

using namespace std;

int total_cnt = 0;

bool voting_readcount(std::istream& r, int& total_cnt) {

    return true;
}

bool voting_read(std::istream& r, int& total_cnt, int& can_cnt, string[] names, int& votes) {
    
    int it = 0;
    
    return true;
}


int main() {
    using namespace std;
    cout << "main" << endl;

    return 0;
}
